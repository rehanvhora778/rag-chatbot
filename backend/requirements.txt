# ============================================================
# AI Multi-Document RAG Chatbot — Full Requirements
# ============================================================

# Core Framework
Django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.4.0

# Authentication
djangorestframework-simplejwt==5.3.1
# Verifies Google Sign-In ID tokens against Google's public keys
google-auth==2.35.0
# google.auth.transport.requests imports this at module load. It used to arrive
# only as a transitive dependency of transformers, which made the import order
# depend on what else happened to be installed. Declared explicitly so it does not.
requests==2.34.2

# Database — PostgreSQL + pgvector (the target store). psycopg 3 is what
# Django 4.2 uses when both are installed. dj-database-url parses DATABASE_URL.
psycopg[binary]==3.2.3
pgvector==0.3.6
dj-database-url==2.3.0

# Background processing — document ingestion runs on a Celery worker instead of
# a daemon thread inside the web process.
celery[redis]==5.4.0
redis==5.2.1

# MongoDB
pymongo[srv]==4.8.0
dnspython==2.6.1

# Environment
python-decouple==3.8

# Document Processing
PyMuPDF==1.24.9
python-docx==1.1.2
chardet==5.2.0
filetype==1.2.0

# ML / Embeddings / Vector Search
sentence-transformers==3.1.1
faiss-cpu==1.8.0
numpy==1.26.4
# Fast embedding inference. The DirectML build uses any Windows GPU (incl. AMD
# iGPUs); on other platforms the plain CPU build is used. sentence-transformers
# stays as the fallback backend (EMBEDDING_BACKEND=torch).
onnxruntime-directml==1.24.4; sys_platform == 'win32'
onnxruntime==1.27.0; sys_platform != 'win32'

# PDF Export — Markdown -> HTML -> PDF document pipeline
markdown==3.7
Pygments==2.18.0
xhtml2pdf==0.2.16
reportlab==4.5.1
# Optional: install WeasyPrint for the highest-fidelity output. It needs native
# GTK/Pango libraries (easy on Linux/macOS, fiddly on Windows). When it is not
# importable the exporter automatically falls back to xhtml2pdf.
# weasyprint==62.3

# Utilities
Pillow==10.4.0

# Static file serving
whitenoise==6.7.0

# Groq LLM (answer generation + vision OCR for scanned PDFs)
groq==1.4.0
